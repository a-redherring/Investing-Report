# Copy to a location outside the repository, for example:
# /etc/investing-advice/secrets.env
# Never commit the populated file.
FINNHUB_API_KEY=

# Free, official, instant signup at https://fredaccount.stlouisfed.org/apikeys
# -- used by fetch-macro (raw macro indicators, not a regime label).
FRED_API_KEY=

# Optional explicit paths for a home-server deployment.
INVESTMENT_SYSTEM_CONFIG_DIR=
INVESTMENT_SYSTEM_SNAPSHOT_DB=
